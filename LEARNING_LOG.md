# LEARNING LOG / 学習記録

体調管理CSV集計CLI（Python Stage 2-2）の開発を通じた学習記録です。

---

## 1. プロジェクト概要

`data/health_data.csv` に記録した血圧・体重データを読み込み、CLIで集計・表示するツール。

- CSVの読み込み（`csv.DictReader`）
- 1行1 dict のリストでデータ管理
- 平均・最高・最低の集計
- 朝（morning）／夜（evening）別の集計
- 型ヒントを意識した関数設計
- pytest による単体テスト

---

## 2. 自分で実装した部分

開発の主要なコードはすべて自分で書いた。

| ファイル | 実装内容 |
|---|---|
| `src/calc.py` | CSV読み込み・集計ロジック（全関数） |
| `src/main.py` | メニュー表示とCLIループ、各機能の表示処理 |
| `tests/test_calc.py` | 集計関数の単体テスト（モック含む） |

### 具体的に自分で考えて実装したこと

- `load_data`：`csv.DictReader` での読み込み、`int`/`float` への型変換、不正行のスキップ判定
- `calc_stats` を汎用関数にして、`calc_weight_stats` / `calc_blood_pressure_stats` から再利用する設計
- `filter_data_by_period`：リスト内包表記での朝夜フィルタ
- テストのモック：`FakeDictReader` を自作し、`monkeypatch` で `csv.DictReader` を差し替え
- `mock_open` と `capsys` を使った、ファイル操作・標準出力のテスト
- バグ修正：`pop` による削除をやめ、正常行だけを `append` で集める方式に変更
- TDD：バグ再現テストを先に書き、失敗（赤）を確認してから修正

---

## 3. AIに頼った部分

| 場面 | 頼った内容 |
|---|---|
| 仕様整理 | 仕様の不足・曖昧点の洗い出し（17項目の疑問提示）。決定は自分で行った |
| README レビュー | 概要・フォルダ構成・Notes の食い違い指摘 |
| 用語説明 | `colorama` とは何か、使い方 |
| コードレビュー | 全体確認、良い点・改善点の指摘 |
| バグ発見 | `load_data` の「反復中に `pop`」によるインデックスずれの指摘と再現 |
| 進め方の助言 | 実装順（集計→テスト→CLI）、テストファーストの方針 |

AIには「答えのコード」ではなく、**気づきのヒントと方向性**をもらう形で進めた。

---

## 4. 学習したこと

### CSV・データ管理
- `csv.DictReader` は1行を dict として返す（Javaの `Map<String, String>` 1行分に近い）
- CSVの値はすべて文字列で読まれるので、読み込み時に `int`/`float` へ変換する
- ネストせず「1行1 dict のリスト」で持つとシンプルで扱いやすい

### 集計・設計
- 汎用関数（`calc_stats`）を1つ作り、各所から呼ぶと再利用できる
- リスト内包表記でのフィルタ（Javaの `stream().filter()` に相当）
- 型ヒントは引数・戻り値に付けるだけでも可読性が上がる

### テスト（今回一番苦労した点）
- `monkeypatch` で標準ライブラリ（`csv.DictReader`）を差し替えられる
- `mock_open` でファイルの中身を擬似的に与えられる
- `capsys.readouterr()` で標準出力を検証できる（呼ぶたびにバッファがクリアされる点に注意）
- テスト関数の引数に `capsys` を書き忘れると `NameError` になる

### バグと品質
- **反復中のリスト削除は危険**：`for ... enumerate(data)` 中に `data.pop()` するとインデックスがずれ、要素を飛ばす（Javaの `ConcurrentModificationException` と同じ原則）
  - 解決：削除ではなく「正常な行だけを新しいリストに集める」
- 不正行が1件だけのテストではこのバグは隠れる → **複数件**を混ぜたテストで検出できた
- 実装のメッセージ文言を変えたら、**テストの期待文字列もセットで変える**必要がある

### 開発の進め方
- 仕様を README に整理してから実装に入ると迷いが減る
- TDD：バグの再現テストを先に書き、「赤を見てから緑にする」と修正を証明できる
- エラーメッセージを読めば、失敗の原因（期待値と実際の差分）を特定できる

---

## 5. 振り返り

- 一番の壁はモックだったが、`FakeDictReader` の自作で乗り越えられた
- バグ修正を「テストファースト」で進め、再現→修正→検証のサイクルを完走できた
- エラーメッセージを手がかりに、6件の失敗を一度に直せた

### 次に挑戦したいこと
- `TypedDict` や `dataclass` でレコードの型をより明確にする
- 行番号とファイル実行番号の対応（ヘッダー考慮）など、メッセージの精度向上
- `colorama` を使った出力の見やすさ改善
